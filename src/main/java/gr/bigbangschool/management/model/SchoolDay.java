package gr.bigbangschool.management.model;

/**
 * Defines the school days used by the timetable.
 *
 * This enum provides a predefined set of weekdays that can
 * be assigned to timetable entries when scheduling lessons.
 */
public enum SchoolDay {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY
}